#include <stdio.h>
/*
int main(void)
{
char *str = "He is my best friend";
int capCnt = 0;

while (*str != '\0')
{
if (IsCapital(*str))
capCnt++;

str++;
}

printf("대문자 개수 : %d \n", capCnt);


return 0;
}

int IsCapital(char ch)
{
if ('A' <= ch && ch <= 'z')
return 1;
else
return 0;
}
*/